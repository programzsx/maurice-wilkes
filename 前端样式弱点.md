## 硬编码颜色泛滥，缺少 CSS 自定义属性设计 token

现象：style.css 全文没有定义任何颜色 token，`--` 变量为零，全部使用字面量十六进制与 rgba，例如 `:root` 的 #17211b/#eef1eb（第 2-3 行）、`.sidebar` 的 #1f3328/#f7f6ef（第 42-43 行）、强调色 #e3c15a 散落在第 98、171、197 行，辅助灰 #667267 出现在第 239、253、334、361、382、443 行，错误色 #b3261e/#7d1a14/#f7e3df 出现在第 295-298 行，仅 `.noun-row span` 相关就用了 #8b6d16（第 370、396 行）。位置：web/src/style.css 全文。影响：换主题、调色需要全文搜索替换，同一语义色（卡片底色、次要文字）容易因手误出现近似但不相同的值，无法批量做主题切换或暗色适配。建议：在 `:root` 中定义 `--color-surface`、`--color-accent`、`--color-text-muted`、`--color-danger` 等语义变量，所有规则引用变量，为后续主题化和暗色模式打基础。

## 完全没有暗色模式支持

现象：整套配色（米白 #fffefa 面板、深绿侧栏 #1f3328、纸黄背景 #eef1eb）都是写死在具体选择器里的，没有 `prefers-color-scheme` 媒体查询，`index.html` 也没有 `<meta name="color-scheme">` 或 `<meta name="theme-color">`。位置：web/src/style.css 全文、web/index.html 第 3-7 行。影响：系统深色模式下表单控件、滚动条会由浏览器按默认 scheme 渲染，与页面浅色设计出现割裂；对夜间使用词典类工具的用户不友好。建议：先把颜色抽成变量，再用 `@media (prefers-color-scheme: dark)` 覆盖变量值，并在 `:root` 或 meta 中声明 `color-scheme` 使原生控件跟随。

## 魔法数字 calc(100vh - 116px) 等脆弱计算

现象：`.noun-list, .detail-panel` 使用 `min-height: calc(100vh - 116px)`（style.css 第 310 行），116px 是对 `.content` 的 padding、工具栏、间隙高度的硬编码求和；类似的还有 `22px` 图标列（第 260 行）、textarea `min-height: 132px`（第 166 行）、`.empty-state` 的 `min-height: 220px`（第 455 行）、`.active-heading` 的 `min-width: 190px`（第 235 行）。影响：只要调整 `.content` 的 padding 或工具栏换行，116px 就会失真，导致面板过高出现页面级滚动或过矮贴边；错误提示 `.error-line` 出现时同样会破坏这个假设。建议：改用 flex/grid 的 `min-height: 0` + 自适应拉伸，或把间距统一为 token 后用 `calc()` 基于 token 组合，避免裸数字。

## 移动端视口单位未使用 dvh

现象：`body { min-height: 100vh }`（style.css 第 15-19 行）、`.app-shell { min-height: 100vh }`（第 31-35 行）都使用传统 `100vh`。影响：在 iOS Safari、Android Chrome 等动态工具栏浏览器上，100vh 大于实际可视高度，底部内容（如分页按钮、新建按钮）可能被地址栏遮挡，需要滚动才能触达。建议：以 `min-height: 100dvh` 为主、`100vh` 作为旧浏览器回退，`@supports` 不需要时可双声明。

## 全局标签选择器造成样式污染

现象：`* { box-sizing: border-box }`（style.css 第 11-13 行）尚可接受，但 `button { cursor: pointer }`（第 27-29 行）、`input, textarea` 的完整外观重置（第 153-162 行）、`label { display: grid; gap: 8px }` 与 `label span`（第 143-151 行）都直接作用于裸标签，没有类名或作用域约束。影响：引入任何第三方组件（日期选择器、富文本、图标按钮组）或新增页面片段时都会被这套全局样式命中，产生难以追溯的覆盖；`label span` 这种后代选择器尤其危险，未来在 label 内再放一个装饰 span 会被强制 13px 浅色。建议：给表单控件加类名（如 `.field-input`、`.field-label`），reset 仅保留 box-sizing 与 margin 清零。

## 覆盖式写法造成特异性纠缠

现象：`.search-box input` 用 `border: 0; padding: 8px 0; background: transparent; box-shadow: none`（style.css 第 270-275 行）逐条抵消第 153-162 行给裸 input 设置的边框、内边距、背景；`.search-box button, .pager button`（第 277-282 行）再覆盖全局 button 的部分属性。影响：这类"先全局设、再局部删"的写法使属性的真实来源难以追踪，改一处全局 input 样式可能悄悄破坏搜索框，维护成本随页面复杂度上升。建议：让搜索框输入与普通表单输入使用不同的类，各自声明需要的最小属性集，避免依赖覆盖顺序。

## 按钮基础样式重复且 disabled 三处重复

现象：`.primary-button, .icon-text-button, .search-box button, .pager button, .icon-button` 共享的 `border: 0; border-radius: 6px; min-height: 40px` 已用长选择器组声明（style.css 第 175-183 行），但 `border-radius: 6px` 在 `.type-button`（第 89 行）、`input/textarea`（第 157 行）、`.search-box`（第 266 行）等处又各写一遍；`:disabled` 样式也在 `.primary-button:disabled, .icon-text-button:disabled, .pager button:disabled`（第 201-206 行）三处枚举，新增按钮类型时容易漏掉。影响：圆角、禁用态这类设计 token 性质的值分散多处，一旦调整会出现视觉不一致。建议：定义 `--radius-sm/--radius-md` 变量与统一的 `.btn` 基类，禁用态用 `:disabled` 全局或类级规则一次覆盖。

## 多数交互控件缺少 hover/active 反馈

现象：`.type-button`（第 96-101 行）和 `.noun-row`（第 346-349 行）有 hover 态，但 `.primary-button`、`.icon-text-button`、`.icon-button`、`.pager button`、`.search-box button` 都没有定义 hover、active（按下）状态，也没有全局 `transition`，点击与否在视觉上无变化（仅 loading 时整体 disabled）。影响：工具栏是本应用的高频操作区，缺少反馈会让用户不确定点击是否生效，界面显得迟钝。建议：为按钮统一加 `transition: filter/opacity` 或 hover 提亮、active 轻微下压，disabled 用 `cursor: not-allowed` 之外的视觉降饱和。

## 可点击列表行缺少键盘可达性与焦点样式

现象：词条列表用 `<article className="noun-row" onClick={...}>`（main.jsx 第 338-350 行）渲染，CSS 只有 `.noun-row:hover, .noun-row.active`（style.css 第 346-349 行）和 `cursor: pointer`（第 343 行），没有 tabindex、role 或 keydown 处理，也没有 `:focus-visible` 样式。影响：键盘用户和读屏用户完全无法选择词条，`cursor: pointer` 造成的"可点击"暗示在触屏上也不成立，是典型的无障碍缺陷。建议：改用 `<button>` 或至少加 `role="button" tabIndex={0}` 与回车/空格处理，并补充 `.noun-row:focus-visible` 的可见焦点环。

## 图标按钮与搜索框缺少可访问名称

现象：取消编辑、编辑、删除三个图标按钮只依赖 lucide 图标和 `title="取消编辑"/title="编辑"/title="删除"`（main.jsx 第 250、377-381 行），`title` 不是可靠的 accessible name；搜索输入框仅靠 placeholder 说明用途（main.jsx 第 304-308 行），聚焦输入后提示消失；错误提示 `<div className="error-line">`（main.jsx 第 324 行）没有 `role="alert"`，不会触发读屏播报。影响：读屏用户听到"按钮""编辑文本框"却不知道其用途，错误信息也会被静默忽略。建议：图标按钮补 `aria-label`，搜索输入加 `aria-label="搜索"`，错误容器加 `role="alert"` 并考虑错误时滚动定位。

## line-clamp 依赖 -webkit 私有属性

现象：`.noun-row p` 使用 `display: -webkit-box; -webkit-box-orient: vertical; -webkit-line-clamp: 2`（style.css 第 357-366 行）实现两行截断。影响：虽然当前主流浏览器都兼容这套私有实现，但标准 `line-clamp` 语法尚未在此回退，混合内容未来若变更（如改用 `box-decoration-break` 配合）容易踩坑；这段私有属性对不支持的引擎会导致描述完整展开，撑破两行布局。建议：保留现有写法的同时评估直接引入 `-webkit-line-clamp` 的标准对应属性，或用组件库统一截断实现，并加注释标注浏览器支持基线。

## 无效的 letter-spacing: 0 冗余声明

现象：`letter-spacing: 0` 在 `.brand h1`（style.css 第 57 行）、`.active-heading h2`（第 249 行）、`.noun-row h3`（第 354 行）重复出现三次，而 `letter-spacing` 的默认值就是 `normal`（等同于 0），没有前面的 letter-spacing 被覆盖，属于无效果声明。影响：这类"随手归零"的属性暗示作者是在靠肉眼调样式，代码里累积的空转属性会误导后来者以为存在设计意图（例如刻意紧排）。建议：删除无效果声明；若确有排版需求，用 `--tracking` token 显式表达。

## 超宽屏缺少内容宽度约束

现象：`.content`（style.css 第 221-227 行）与 `.workspace`（第 301-306 行）没有任何 `max-width` 或居中约束，`.app-shell` 右栏为 `1fr`，`.detail-top h2` 的 `clamp(28px, 5vw, 56px)`（第 401 行）是全页唯一的流式字号。影响：在 2560px 及以上的显示器上，描述正文（18px、line-height 1.75）一行可长达约 170 个字符，`white-space: pre-wrap` 又禁止了自然断行优化，长描述可读性骤降；列表行也会被拉得极宽。建议：给 `.content` 或 `.description` 设置 `max-width`（如 `70ch`/`960px`），并给 `.description` 增加 `max-width` 或分栏。

## 单一扁平 CSS 文件缺乏组织约定

现象：全部 513 行样式集中在 `web/src/style.css`，reset、布局、侧栏、列表、详情、响应式按书写顺序堆叠，仅靠 `.error-line` 附近的空行分组，类名采用松散的 BEM 风格（如 `.icon-button.danger`）但没有命名空间约定，`.type-nav`、`.noun-form` 等按页面区域而非组件划分。影响：以当前规模尚可维护，但每加一个页面区域都要在同一个文件里找插入位置，类名（`.pager`、`.toolbar` 这类通用词）与其他组件冲突的风险随之上升，删除功能时也难以判断哪些规则成了死代码。建议：按组件拆分为多个 CSS 文件或采用 CSS Modules 的 `.module.css`，通用变量与 reset 独立成 `tokens.css`/`base.css`。

## .content overflow:hidden 属于隐式布局 hack

现象：`.content { overflow: hidden }`（style.css 第 226 行）配合 `.workspace { min-height: 0 }`（第 305 行）和面板 `min-height` 一起工作，意图是防止子元素溢出撑破布局，但注释为零。影响：`overflow: hidden` 会创建新的格式化上下文并裁剪溢出内容，未来在 `.content` 内加入绝对定位的下拉、气泡提示（如日期选择、确认浮层）时会被无声截断；后人也不敢轻易移除这行。建议：明确布局意图，用 `min-width: 0`/`min-height: 0` 处理 grid 溢出，裁剪需求集中在真正需要滚动的 `.noun-list`、`.detail-panel` 上（它们已有 `overflow: hidden`/`auto`）。

## 加载与空状态视觉同质，缺少状态区分

现象：加载中、没有数据、未选择词条三种状态都复用 `.empty-state`（style.css 第 452-457 行），仅文本不同（main.jsx 第 333-336、410 行），没有 spinner、骨架屏或图标；`.list-meta` 的总数在加载期间也不会显示占位。影响：弱网下"加载中"三个静态文字容易被理解为"没有数据"，用户会误判列表为空而尝试重复操作；相邻的刷新按钮虽有 disabled 态但视觉变化很弱（仅 opacity 0.55）。建议：为加载态增加旋转图标或骨架行，为空态提供引导性图标与主操作（如"收入第一个名词"），让三种状态在视觉上可区分。
